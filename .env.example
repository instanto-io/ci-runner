# Copy to .env and set for this machine.
RUNNER_HOST=my-machine
# Extra labels are comma-separated; the runner adds self-hosted, Linux and its architecture itself.
RUNNER_LABELS=instanto-container
# Optional: pull a published image instead of building from this directory.
# CI_RUNNER_IMAGE=registry.example:5000/instanto-docker/ci-runner:latest
